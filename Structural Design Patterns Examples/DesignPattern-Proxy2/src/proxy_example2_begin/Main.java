package proxy_example2_begin;

public class Main {

	/*
	public static void main(String[] args) {
		
		// The problem here is that we are adding many items to the inventory but we are using 
		// them only when store.printInventory() and even if we are printing we are creating them uselessly
		// we should use proxy pattern so when an inventory is created in the main class it doesn't create a list
		// of items as that point so we can reduce memory usage.
		Inventory inventory = new SuperstoreInventory();
		Store store = new Store("Healthy Wholefoods", "Los Angeles", inventory);
		
		store.printName();
		store.printLocation();
		store.printInventory();
	}
	*/

}
